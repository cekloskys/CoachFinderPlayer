import { View } from 'react-native';
import styles from './styles';

const Package = () => {

    return (
        <View style={styles.page}>
          
        </View>
      );
}

export default Package;